# SQL Server Configuration (Trusted Connection)
DB_DIALECT=mssql
MSSQL_CONNECTION_STRING=Server=NGOEE\SQLEXPRESS;Database=GraduationDB;Trusted_Connection=True;TrustServerCertificate=True;

# OR use individual parameters:
DB_SERVER=NGOEE\SQLEXPRESS
DB_NAME=GraduationDB
DB_TRUSTED_CONNECTION=true
DB_TRUST_CERTIFICATE=true

# PostgreSQL Configuration (fallback)
DATABASE_URL=postgresql://user:password@localhost:5432/graduation_invite

# Application
NODE_ENV=development
